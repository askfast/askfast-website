define(
  ['controllers/controllers'],
  function (controllers)
  {
    'use strict';

    controllers.controller ('register',
      [
        '$scope',
        function ($scope)
        {
        }
      ]
    );
  }
);